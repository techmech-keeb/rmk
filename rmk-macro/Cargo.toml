[package]
name = "rmk-macro"
version = "0.8.0"
edition = "2024"
authors = ["Haobo Gu <haobogu@outlook.com>"]
description = "Proc-macro crate of RMK"
homepage = "https://github.com/rmk-rs/rmk"
repository = "https://github.com/rmk-rs/rmk"
license = "MIT OR Apache-2.0"
resolver = "2"

[dependencies]
rmk-config = { path = "../rmk-config", version = "=0.7.0"}
rmk-types = { path = "../rmk-types", version = "=0.3.0", features = ["_codegen"] }
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
proc-macro2 = "1.0"
darling = "0.23"
cargo_toml = "1"
strum = { version = "0.28", default-features = false, features = ["derive"] }
# Simulator scenario parsing only (`_simulator`). Rynk payloads are handed to
# the test as JSON, so the request type is named once, in the test crate.
toml = { version = "1", optional = true }
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }

[features]
## Enable hardware watchdog code generation
watchdog = []
## Expose the `run_tests!` simulator-scenario macro (rmk test suite only)
_simulator = ["dep:toml", "dep:serde", "dep:serde_json"]
dfu_rp = []
## Enable nRF DFU firmware update code generation
dfu_nrf = []
dfu_split = []

[lib]
proc-macro = true
doctest = false

[dev-dependencies]
macrotest = "1.0"
trybuild = "1.0"
rmk = { path = "tests/rmk", package = "rmk-mock" }

# VENDOR PATCH (olsk60, hi-res scroll): same usbd-hid pin as rmk/Cargo.toml —
# [patch] only applies at the root manifest, and this crate builds rmk as a
# dependency. Drop when usbd-hid > 0.10.1 ships.
[patch.crates-io]
usbd-hid = { git = "https://github.com/twitchyliquid64/usbd-hid", rev = "b971d4c3b1c88ba9b58e7625f454e549e60774c9" }
